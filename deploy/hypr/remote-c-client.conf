# Remote C behaves like the other utility windows launched from Waybar.
windowrule = float on, match:class ^(io\.github\.ab2348\.RemoteC)$
windowrule = persistent_size on, match:class ^(io\.github\.ab2348\.RemoteC)$
