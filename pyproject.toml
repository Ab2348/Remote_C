[project]
name = "remote-c"
version = "0.5.0"
description = "Control remoto local para Arch Linux"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.139.0",
    "uvicorn[standard]>=0.51.0",
]
